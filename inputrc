# Load the systemwide inputrc
$include /etc/inputrc

# Bracketed paste prevents the automatic execution of any commands
# that are pasted into the terminal.
# For more info: https://univers-libre.net/posts/safe-paste-in-terminals.html
set enable-bracketed-paste on

# Add a slash to every completion which is a symbolic link to a directory
set mark-symlinked-directories On

# Show all completions the first time tab is pressed and a partial completion can be made
set show-all-if-ambiguous On

# Show all completions the first time tab is pressed, but no partial completion can be made
set show-all-if-unmodified On

# Ignore case for autocomplete
set completion-ignore-case On

# Use tab to cycle forward through completions
Tab:menu-complete

# Use shift+tab to cycle backward through completions
"\e[Z":menu-complete-backward

# Display the common prefix between the entered text and completions
set menu-complete-display-prefix on

# When performing autocomplete while the cursor is in the middle of a word,
# do not insert characters from the completion that match characters that
# come after the cursor. This way they don't get duplicated.
set skip-completed-text on

# use ctrl+b to go the beginning of a line
"\C-b":beginning-of-line
