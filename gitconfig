[user]
    name = Biggie Emmanuel
	  email = hi@bigo.lu
[core]
    editor = vim
    pager = "command -v delta > /dev/null && delta --hyperlinks || less"
[push]
    default = current
[alias]
    all = add -A
    p = pull
    s = status
    ss = status --short
    c = commit
    a = add
    d = diff
    undo = checkout --
    some = add -u
    undocommit = reset "HEAD^"
    co = checkout
    ui = "!gitui"
[init]
    defaultBranch = master
[merge]
    conflictstyle = diff3
[diff]
    colorMoved = default
[interactive]
    diffFilter = "delta --color-only"
[include]
    path = ~/.dotfiles/themes.gitconfig
[delta]
    line-numbers = true
    navigate = true  # use n and N to move between diff sections
    light = true
