# If not running interactively, exit this script.
# Reasoning is explained here: https://unix.stackexchange.com/questions/257571/why-does-bashrc-check-whether-the-current-shell-is-interactive
if [ -z "$PS1" ]; then
	return
fi

# Use fish in place of bash for an interactive shell. Keep this line at the bottom of the file since
# nothing after this line will be executed.
which fish >/dev/null 2>&1 && SHELL="$(which fish)" exec fish
