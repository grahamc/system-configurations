colors:
  cyan: 6
  gray: 8
  green: 2
  red: 1
  yellow: 3

pre-commit:
  piped: true
  commands:
    generate-toc-readme:
      priority: 1
      glob: "README.md"
      run: direnv exec "$PWD" just generate-toc
    format:
      priority: 2
      run: direnv exec "$PWD" just format

